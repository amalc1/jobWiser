import { Box } from "@mui/material";
import React from "react";

const ProfileLeft = () => {
  return (
    <Box flex={1} sx={{ backgroundColor: "lavender" }}>
      ProfileLeft
    </Box>
  );
};

export default ProfileLeft;
