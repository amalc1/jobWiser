import {createTheme} from "@mui/material";

export const theme = createTheme({
    palette:{
      primary:{
        main: "#F5F5F5",   // gray like
        blue: '#4540DB',
        light: "#413AFD"  //blue
      },
      secondary:{
        main: '#15c630',
      },
      otherColor:{
        main:"#999"
      }
    }
  })
